#!/usr/bin/env bash
#MISE description="Pull all loom skills from skills.sh globally"
set -euo pipefail

echo "⚠ skills.sh may have a cache delay — if you just pushed, wait 2 minutes and retry"
cd ~ && npx skills add freeacger/loom -y -g
echo "✓ all loom skills pulled"
